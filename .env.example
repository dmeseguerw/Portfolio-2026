# Resend API key, used by app/api/contact/route.ts to send contact form
# submissions via email. Get a free-tier key at https://resend.com/api-keys
# after creating an account. Required in both local .env.local and the
# Vercel project's environment variables for the contact form to work.
RESEND_API_KEY=
